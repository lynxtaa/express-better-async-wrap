{
  "name": "express-better-async-wrap",
  "version": "0.1.0",
  "description": "Allows use of async functions as Express router handlers",
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "module": "es/index.js",
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "pre-push": "npm run lint"
    }
  },
  "lint-staged": {
    "*.{js,jsx,json}": [
      "prettier --write",
      "git add"
    ]
  },
  "scripts": {
    "build": "babel src -d lib",
    "prebuild": "rimraf lib",
    "lint": "eslint src/**/*.js",
    "preversion": "npm run build",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex Kondratyuk <lynxtaa@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.0.0-beta.49",
    "@babel/core": "^7.0.0-beta.49",
    "@babel/preset-env": "^7.0.0-beta.49",
    "eslint": "^4.19.1",
    "husky": "^1.0.0-rc.7",
    "jest": "^23.0.0",
    "lint-staged": "^7.1.2",
    "prettier": "^1.12.1",
    "rimraf": "^2.6.2"
  }
}
